# Dependency directories
node_modules/
jspm_packages/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# Environment variables (CRITICAL: Never commit your DB secrets)
.env
.env.test
.env.local
.env.development.local
.env.test.local
.env.production.local

# Prisma
/prisma/migrations/
# Only ignore migrations if you want to reset DB often during early dev, 
# but usually, you keep them. However, ALWAYS ignore the SQLite db if using it:
*.db
*.db-journal

# OS Metadata
.DS_Store
Thumbs.db

# IDE / Editor folders
.vscode/
.idea/
*.swp
*.swo

# Build/Dist folders
dist/
build/